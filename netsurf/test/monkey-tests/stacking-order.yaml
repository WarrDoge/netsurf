title: positioned boxes paint over the flow, in z-index order
group: basic
steps:
- action: launch
- action: window-new
  tag: win1
- action: navigate
  window: win1
  url: file://${CWD}/test/js/stacking-order.html
- action: block
  conditions:
  - window: win1
    status: complete
- action: plot-check
  window: win1
  checks:
  - text-contains: zindex minus in flow second positioned first zindex one zindex two
- action: quit
